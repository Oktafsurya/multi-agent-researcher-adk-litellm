# Agent SDK
google-adk==1.3.0

# LiteLLM — gateway, caching, guardrails
litellm[proxy]

# AWS Bedrock client (used by LiteLLM)
boto3>=1.34.0
botocore>=1.34.0

# Langfuse — observability
langfuse>=2.36.0

# Redis — caching backend
redis>=5.0.0

# Environment & config
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Async HTTP
httpx>=0.27.0

# PostgreSQL — persistent memory store
psycopg2-binary>=2.9.0

# Guardrail service
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
